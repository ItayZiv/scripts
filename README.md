```bash
# Normal usage
bash <(wget -qO - https://scripts.itayziv.dev/mirror.sh) {chart}
# With version and values
bash <(wget -qO - https://scripts.itayziv.dev/mirror.sh) {chart} {version} {valuefile}
```
